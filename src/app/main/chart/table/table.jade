.main__chart__view__table
  table.table.table-striped
    thead
       tr
        th(translate) totals
        th(ng-repeat="vehicle in main.vehicles", style="width: 35%")     
          i.fa.fa-car
          span &nbsp;
          span(ng-bind-html="'vehicle_name' | translate:main.getVehicleOpts(vehicle)")
    tbody
      tr
        td(translate) tco_table
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.total_cost) }}€
      tr
        td(translate) co2_title
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.CO2) }}t CO₂
          
  table.table.table-striped
    thead
       tr
        th(translate) details
        th(ng-repeat="vehicle in main.vehicles", style="width: 35%")        
          i.fa.fa-car
          span &nbsp;
          span(ng-bind-html="'vehicle_name' | translate:main.getVehicleOpts(vehicle)")
    tbody                  
      tr 
        td(translate) vehicle_basis_cost  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.vehicle_basis_cost) }}€
      tr 
        td(translate) cash_bonus  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.cash_bonus) }}€
      tr 
        td(translate) charging_infrastructure  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.charging_infrastructure) }}€
      tr 
        td(translate) energy_costs  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.energy_costs) }}€
      tr 
        td(translate) lubricant_costs  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.variable_costs.lubricant_costs) }}€
      tr 
        td(translate) maintenance_costs  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.variable_costs.maintenance_costs) }}€
      tr 
        td(translate) amortization  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.variable_costs.amortization) }}€
      tr 
        td(translate) check_up  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.fixed_costs.check_up) }}€
      tr 
        td(translate) insurance  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.fixed_costs.insurance) }}€
      tr 
        td(translate) car_tax  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.fixed_costs.car_tax) }}€
      tr 
        td(translate) amortization_vehicle  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.amortization_vehicle) }}€
      tr 
        td(translate) residual_value  
        td(ng-repeat="vehicle in main.vehicles")
          | {{ table.number(vehicle.TCO.residual_value) }}€
